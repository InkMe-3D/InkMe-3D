name: 🚀 Sync folders to deploy branches

on:
  push:
    branches:
      - main
    paths:
      - 'admin/**'
      - 'client/**'
      - 'server/**'
      - '3d-custom/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        folder: [admin, client, server, 3d-custom]

    steps:
      - name: ⬇️ Checkout main
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: ⚙️ Setup Git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: 🚚 Sync ${{ matrix.folder }} to ${{ matrix.folder }} branch
        run: |
          FOLDER=${{ matrix.folder }}
          BRANCH=$FOLDER

          echo "📦 Syncing folder '$FOLDER' to branch '$BRANCH'"

          # Copy folder content into temp
          cp -r "$FOLDER" temp-folder

          # Fetch & checkout target branch
          git fetch origin
          if git show-ref --verify --quiet refs/remotes/origin/$BRANCH; then
            echo "✅ Branch $BRANCH exists. Checking out..."
            git checkout -B "$BRANCH" "origin/$BRANCH"
          else
            echo "🆕 Branch $BRANCH does not exist. Creating..."
            git checkout -b "$BRANCH"
          fi

          # Remove all files except .git
          find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +

          # Move files from temp folder into current branch
          cp -r temp-folder/. ./
          rm -rf temp-folder

          # Commit and push if there are changes
          git add .
          git diff --cached --quiet || git commit -m "🔁 Sync from main: $FOLDER"
          git push origin "$BRANCH"

      - name: ✅ Done
        run: echo "✅ All done syncing ${{ matrix.folder }}!"
